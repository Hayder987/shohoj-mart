import React, { useEffect } from 'react'

const HelpSupport = () => {

  useEffect(() => {
      window.scrollTo(0, 0);
    }, []);

  return (
    <div>HelpSupport</div>
  )
}

export default HelpSupport